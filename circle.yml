machine:
  environment:
    RUST_HOME: $HOME/rust
    PATH: $RUST_HOME/bin:$PATH

dependencies:
  post:
    - ./scripts/install-rust.sh $RUST_HOME
    - ./scripts/install-alsa.sh
    - rustc --version --verbose

  cache_directories:
    - "~/rust"

test:
  override:
    - cargo build
