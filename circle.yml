machine:
  environment:
    RUST_HOME: $HOME/rust
    PATH: $RUST_HOME/bin:$PATH

dependencies:
  post:
    - ./scripts/install-rust.sh $RUST_HOME
    - rustc --version --verbose

  cache_directories:
    - "~/rust"

test:
  override:
    - cargo build
