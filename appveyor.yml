version: 0.2.0-{build}

environment:
  global:
    PROJECT_NAME: hinterland
  matrix:
    - TARGET: i686-pc-windows-gnu
    - TARGET: x86_64-pc-windows-msvc
    - TARGET: x86_64-pc-windows-gnu

install:
  - set PATH=C:\msys64\usr\bin;%PATH%
  - set PATH=C:\msys64\mingw32\bin;%PATH%
  - if "%TARGET%" == "x86_64-pc-windows-gnu" set PATH=C:\msys64\mingw64\bin;%PATH%
  - appveyor-retry appveyor DownloadFile https://win.rustup.rs/ -FileName rustup-init.exe
  - rustup-init.exe --default-host %TARGET% --default-toolchain stable -y
  - set PATH=%PATH%;C:\Users\appveyor\.cargo\bin
  - rustc -Vv
  - cargo -V

build: false

test_script:
  - cmd: 'cargo test'

cache:
  - target
  - C:\Users\appveyor\.cargo

before_deploy:
  - mkdir hinterland
  - mkdir hinterland\assets
  - cp assets\*.png hinterland\assets
  - cp assets\*.json hinterland\assets
  - xcopy assets\maps hinterland\assets
  - xcopy assets\audio hinterland\assets
  - copy target\release\hinterland_bin hinterland\hinterland
  - 7z a ../%PROJECT_NAME%-%TARGET%.zip *

deploy:
  provider: Github
  artifact: /.*\.zip/
  auth_token:
    secure: zjp/gY3ooTtDDpz3Kzi97tLidzfDIPUelcgWhb/mpt3NoSQCkLxWbXx0ujvio45F
  on:
    appveyor_repo_tag: true
    branch:
      - master
